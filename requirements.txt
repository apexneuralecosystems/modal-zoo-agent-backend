requests==2.32.3
opencv-python==4.10.0.84
# PyAV: bundles a modern FFmpeg that decodes real-world Hikvision H.264 RTSP
# streams which opencv-python's bundled FFmpeg fails on (endless
# cabac_init_idc / non-existing PPS errors, zero frames). The live streamer and
# the discovery/probe paths decode with PyAV; cv2 is kept only for JPEG encode.
av==16.0.1
# Aligned with apps/backend/requirements.txt -- both ends must be on the same
# ultralytics line so a .pt produced/cached on the cloud can be loaded on the
# Mac without "Can't get attribute 'Segment26'" or similar class-not-found errors.
ultralytics>=8.4.51
numpy==1.26.4
# zoneinfo (stdlib) needs an IANA tz database to resolve named zones like
# "Asia/Kolkata". Linux/macOS normally have one on the system already; Windows
# does not, so ZoneInfo(name) silently raises ZoneInfoNotFoundError there
# unless this package provides the data. Harmless no-op on platforms that
# already have system tzdata.
tzdata>=2024.1
psutil==6.1.0
websocket-client==1.8.0
boto3>=1.34.0
imageio-ffmpeg>=0.5.0
